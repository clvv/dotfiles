_F_SINK="$HOME/.fasd.log"
_F_MAX=10000000
_F_BACKENDS=native

local sh; for sh in /bin/dash /bin/ksh /bin/mksh /bin/zsh; do
  $sh -c exit && _F_SHELL=$sh && break
  echo "$_F_SHELL" >> "$_F_SINK"
done

# vim: se ft=sh:

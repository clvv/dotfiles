_FASD_SINK="$HOME/.fasd.log"
_FASD_MAX=10000
_FASD_BACKENDS="native recently-used"

local sh; for sh in /bin/dash /bin/ksh /bin/mksh /bin/zsh; do
  $sh -c exit && _FASD_SHELL=$sh && break
  echo "$_FASD_SHELL" >> "$_FASD_SINK"
done

# vim: se ft=sh:

if &t_Co == 256
  set mouse=a
  if filereadable(expand('~/.vim/bundle/Lucius/colors/lucius.vim'))
    let &runtimepath .= ',' . expand('~/.vim/bundle/Lucius')
    colorscheme lucius
  endif
endif

" vim: se ft=vim:
